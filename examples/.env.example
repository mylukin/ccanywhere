# CCanywhere Environment Configuration
# 
# This file is for users who prefer environment variables over config files.
# Copy this file to .env and fill in your actual values.
# 
# Note: Config files (ccanywhere.config.json) take precedence over environment variables.
# ==================================================================================

# Repository Configuration (Optional - Auto-detected from .git)
# If your project is a Git repository, these will be auto-detected.
# The project name will be extracted from the URL for storage organization.
# Example: https://github.com/owner/repo → storage path: diffs/owner/repo/
# REPO_URL=https://github.com/mylukin/my-project  # Optional - used for project path
# REPO_KIND=github                                # Optional - auto-detected
# REPO_BRANCH=main                                # Optional - auto-detected

# Deployment Configuration
DEPLOYMENT_WEBHOOK_URL=https://deploy.example.com/api/webhook/deploy

# Notification Channels (comma-separated: telegram,dingtalk,wecom,email)
NOTIFY_CHANNELS=telegram

# Telegram Configuration
BOT_TOKEN_TELEGRAM=YOUR_BOT_TOKEN
CHAT_ID_TELEGRAM=YOUR_CHAT_ID

# DingTalk Configuration (Optional)
# DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN

# WeCom Configuration (Optional)
# WECOM_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY

# Email Configuration (Optional)
# EMAIL_TO=admin@example.com
# EMAIL_FROM=noreply@example.com

# SMTP Configuration (Optional - uses local mail if not specified)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false  # true for port 465, false for other ports
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Build Configuration
BASE=origin/main
LOCK_TIMEOUT=300
CLEANUP_DAYS=7
# EXCLUDE_PATHS=.artifacts,node_modules,dist  # Optional: comma-separated paths to exclude

# Test Configuration
TEST_ENABLED=false
PLAYWRIGHT_CONFIG=./playwright.config.ts

# Artifacts Configuration
ARTIFACTS_BASE_URL=https://artifacts.example.com
ARTIFACTS_RETENTION_DAYS=7
ARTIFACTS_MAX_SIZE=100MB

# Storage Configuration
STORAGE_PROVIDER=r2  # Options: r2, s3, oss
STORAGE_FOLDER=diffs  # Storage folder path (default: "diffs")
                      # Files will be stored as: {folder}/{project-name}/{filename}
                      # Example: diffs/mylukin/my-project/diff-7531080.html

# Cloudflare R2 Storage (Default)
R2_ACCOUNT_ID=YOUR_CLOUDFLARE_ACCOUNT_ID
R2_ACCESS_KEY_ID=YOUR_R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=YOUR_R2_SECRET_ACCESS_KEY
R2_BUCKET=my-artifacts-bucket

# AWS S3 Storage (Optional)
# S3_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
# S3_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
# S3_REGION=us-east-1
# S3_BUCKET=my-s3-bucket

# Aliyun OSS Storage (Optional)
# OSS_ACCESS_KEY_ID=YOUR_ALIYUN_ACCESS_KEY_ID
# OSS_ACCESS_KEY_SECRET=YOUR_ALIYUN_ACCESS_KEY_SECRET
# OSS_REGION=oss-cn-hangzhou
# OSS_BUCKET=my-oss-bucket

# Security Configuration (Optional)
# READ_ONLY=false  # Set to true for read-only mode
# LINK_EXPIRY=3600  # Link expiry time in seconds

# Advanced Deployment Configuration (Optional)
# DEPLOYMENT_STATUS_URL=https://deploy.example.com/api/status
# MAX_WAIT=300  # Maximum wait time for deployment in seconds
# POLL_INTERVAL=10  # Polling interval for deployment status in seconds